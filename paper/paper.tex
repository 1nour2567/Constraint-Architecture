% AAAI-27 Demo Track Submission
\documentclass[letterpaper]{article} % DO NOT CHANGE THIS
\usepackage[submission]{aaai2027}  % DO NOT CHANGE THIS
\usepackage[hyphens]{url}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{natbib}
\usepackage{caption}
\urlstyle{rm}
\def\UrlFont{\rm}
\frenchspacing

\title{Constraint Architecture: A Cross-Domain Validated,\\
       Pip-Installable Agent Safety Skeleton\\
       {\small (AAAI-27 Demo Track)}}

\author{
  Xu Renwu (续仁舞)\\
  Huaqiao University\\
  1nour2567@gmail.com
}

\begin{document}
\maketitle

\begin{abstract}
Recent work (LGA, SARC, CAAF, ePCA) establishes multi-layer deterministic
governance as the emerging standard for agent safety.  We build on this
direction with three contributions: (1)~Cross-domain structural convergence---two
agent systems in unrelated domains (security ops, embodied music), built by the
same developer without shared code, converged on the same four-layer
architecture and four structural invariants.  We do not claim true independence;
we report the convergence as post-hoc evidence that the architecture may capture
a real engineering invariant.  (2)~An engineering-grade artifact---\texttt{pip
install constraint-architecture} (v1.5.1): zero dependencies, 1384~lines of
Python, 7~ABCs + 7~Protocols, 10~frozen dataclasses with
\texttt{\_\_post\_init\_\_} value-bound enforcement, 25~automated self-checks.
(3)~Ablation-validated layering---attack-log analysis shows each of the four
constraint layers catches attacks the others miss.  \textbf{Demo:} \texttt{python
-m constraint\_architecture.demo} executes three commands through a live
constraint engine with zero configuration.
\end{abstract}

%------------------------------------------------------------------------
\section{Architecture}
%------------------------------------------------------------------------

Recent work---LGA \cite{ge2026}, SARC \cite{besanson2026}, CAAF \cite{li2026},
ePCA \cite{yang2026}, and the IETF ASA draft \cite{baysal2026}---has established
that multi-layer deterministic governance is the right direction for agent
safety.  We build on this work.  Our skeleton instantiates four deterministic
layers:

\begin{enumerate}
\item \textbf{Reasoning} --- LLM proposes actions; cannot execute or bypass layers.
\item \textbf{Constraints} --- Deterministic validation. Rules are append-only
      (tightened, never removed). Violations raise \texttt{VetoError}
      (structured \texttt{reason}/\texttt{alternative}/\texttt{ref}).
\item \textbf{Execution} --- Tiered: \texttt{AUTO} (read-only),
      \texttt{CONFIRM} (human approval), \texttt{VETO} (never executable).
      Role enforcement at the code layer.
\item \textbf{Audit} --- Immutable SHA256 hash chain with mandatory
      \texttt{prev\_hash} on every \texttt{AuditEvent}.
\end{enumerate}

\textbf{What distinguishes this instantiation.}
(1)~\textit{Post-hoc structural convergence.} Two fully functional agent
systems---Kylin-Agent (security ops on Kylin OS V11) and Malio (embodied AI
music, 800 particles)---were built by the same developer without shared code or
design docs.  After completion, both were discovered to share the same four
layers and four invariants: append-only constraint store, tiered execution with
confirm middle tier, prev\_hash-chained audit, and code-layer role enforcement.
We explicitly note that single-developer authorship is not a true independence
test; the convergence is suggestive, not dispositive.  A third reference
implementation by an independent team would provide stronger evidence, and the
pip-installable skeleton is designed to lower that barrier.

(2)~\textit{Engineering-grade artifact.} The result is not a whitepaper---it is
\texttt{pip install constraint-architecture} (v1.5.1): zero dependencies, 7~ABCs
+ 7~Protocols, 10~frozen dataclasses with \texttt{\_\_post\_init\_\_} validation,
4~custom exceptions with structured fields, and 25~automated self-checks.
Architectural commitments are enforced at the code level---frozen dataclasses
prevent veto flipping, \texttt{\_\_post\_init\_\_} bounds \texttt{risk\_score}
to 0--9, \texttt{AuditEvent} requires \texttt{prev\_hash} at construction.

(3)~\textit{Ablation-validated layering.} Attack-log analysis provides
per-layer contribution estimates (see Supplementary Material): removing T0
exposes the LLM to all obfuscated payloads; removing T2 allows 3 of 16 attacks
(largely tool-parameter crafting and role escalation) to reach the sandbox;
removing T4 eliminates audit-chain replay detection. Each layer catches
attacks the others miss.  The four are not redundant.

%------------------------------------------------------------------------
\section{Engineering Properties}
%------------------------------------------------------------------------

The skeleton exposes 7~ABCs, 7~Protocols, 10~frozen dataclasses,
4~custom exceptions, and 25~automated self-checks. \textbf{Zero dependencies.}

\begin{table}[h]
\centering
\caption{Skeleton v1.5.1 engineering properties.}
\begin{tabular}{lrl}
\toprule
Category & Count & Key Detail \\
\midrule
ABCs (abstract classes) & 7 & Shell, Identity, Scheduler, Memory, Protocol, \newline Tools, + 4 constraint-layer ABCs \\
Protocols (duck-typing) & 7 & One per ABC \\
Frozen dataclasses & 10 of 12 & \verb|__post_init__| validates \verb|risk_score| (0--9), \verb|prev_hash| \\
Custom exceptions & 4 & \verb|VetoError| (reason+alternative+ref), \verb|ExecutionRefused|, \verb|AuditIntegrity| \\
Enums & 4 & Tier(3), EventType(8), MemoryLevel(3) \\
Self-checks & 25 & ABC instantiation, frozen immutability, value bounds \\
\addlinespace
Lines of Python & 1384 & \\
Dependencies & 0 & \\
Python & 3.10+ & \\
\bottomrule
\end{tabular}
\end{table}

%------------------------------------------------------------------------
\section{Reverse Extraction: Two Domains}
%------------------------------------------------------------------------

The architecture was \textit{not} designed top-down. Two systems were built
independently, from scratch, without shared code. After completion, they were
found to share the same four-layer structure.

\textbf{Kylin-Agent} (security ops on Kylin OS V11).  Four deterministic
defense layers---T0 injection detection, T1 risk scoring (1--10 heuristic),
T2 dual-path validation (structured + regex, both must agree), T3 sandbox
(\texttt{sudo -n} whitelist)---then SHA256 audit chain.  \textbf{16 red-team
attacks, 0 breaches.  149 tests.}

Red-team attacks spanned: prompt injection (direct, hex/base64 obfuscation,
Unicode escapes, overflow payloads), role escalation attempts, tool-parameter
crafting, audit-chain replay, and multi-turn context poisoning. T0 intercepted
100\% of obfuscated payloads; T2 caught 3 cases where the LLM output passed
semantic validation but failed raw regex (dual-path disagreement).

\textbf{Malio} (embodied AI music agent).  800 particles, 9 physics systems.
PersonaEngine enforces a central-bank emotional currency model: no code can
directly modify \texttt{energy}/\texttt{warmth}/\texttt{playfulness}.
The same four layers manifest as: MusicAgent reasoning $\rightarrow$ PersonaBounds
constraints $\rightarrow$ core\_action push with role filtering $\rightarrow$
federated SBERT-signed audit.  In one concrete test, the MusicAgent proposed a
\texttt{light\_burst} (a high-energy visual effect) while the persona's energy
was at 0.2.  The LLM, unconstrained, would have executed the action.  The
PersonaEngine vetoed it deterministically---the LLM did not self-censor, nor
could it override the bounds.  Full attack taxonomy and benchmarks are in a
1-page supplementary.

%------------------------------------------------------------------------
\section{Demo Plan \& Video Outline}
%------------------------------------------------------------------------

The AAAI-27 demo is self-contained---attendees run it on their own laptops.
\texttt{python -m constraint\_architecture.demo} runs three commands through
a live constraint engine (auto/block/confirm), zero config.
\textbf{5-minute video:}
\begin{enumerate}
\item \texttt{pip install constraint-architecture} (2~s, zero deps)
\item Attendee proposes a safety rule (e.g., ``block commands touching
      \texttt{/etc/passwd}'') $\rightarrow$ subclass \texttt{ConstraintEngine} live
      $\rightarrow$ rule enforces deterministically
\item \texttt{python -m constraint\_architecture.skeleton} $\rightarrow$
      25 checks pass $\rightarrow$ skeleton validates itself
\item Side-by-side: same 4-layer structure in Kylin-Agent vs.\ Malio
\item ``Third-domain challenge'': attendee proposes a domain
      (healthcare? finance?), we sketch the 4-layer mapping live
\end{enumerate}

Video demonstrates that ``LLM reasons; code decides'' is not a
whitepaper---it is install-and-subclass code.  (Video: {\tt \url{https://github.com/1nour2567/Constraint-Architecture}})

%------------------------------------------------------------------------
\section{Related Work}
%------------------------------------------------------------------------

Recent work---LGA \cite{ge2026}, SARC \cite{besanson2026}, CAAF \cite{li2026},
ePCA \cite{yang2026}, ShieldAgent \cite{zhao2025}, and the IETF ASA
draft \cite{baysal2026}---proposes layered architectures for agent safety.
We explicitly build on this direction but note three gaps that our work fills:

\textbf{Domain-specific constraint engineering.}
Existing frameworks validate safety via benchmark interception rates.
Our constraints---append-only store, \texttt{AUTO/CONFIRM/VETO} tiering,
mandatory \texttt{prev\_hash} chaining, immutable frozen dataclasses with
\texttt{\_\_post\_init\_\_} value-bound enforcement---are engineering
invariants extracted from live systems, not a priori specification.

\textbf{Cross-domain structural validation.}
The four structural invariants named above emerged independently in two
unrelated production systems (security ops and embodied music) that shared
no code, design doc, or development knowledge.  No existing framework reports
this form of convergent-evolution validation from within a single codebase.

\textbf{Engineering-grade artifact, not policy document.}
The result of our extraction is \texttt{pip install constraint-architecture}
(v1.5.1): zero dependencies, 25 automated self-checks, and a one-click demo
(\texttt{python -m constraint\_architecture.demo}).  Architectural commitments
are enforced at the code level---frozen dataclasses prevent veto flipping,
\texttt{\_\_post\_init\_\_} bounds \texttt{risk\_score} to 0--9,
\texttt{AuditEvent} requires \texttt{prev\_hash} at construction.
Layer-isolated evaluation \cite{hu2026} provides the testing methodology;
our skeleton provides the structural guarantee that makes such isolation
meaningful.

\bigskip
\noindent\textbf{PyPI}: \url{https://pypi.org/project/constraint-architecture/}
\quad
\textbf{GitHub}: \url{https://github.com/1nour2567/Constraint-Architecture}


\begin{thebibliography}{}

\bibitem[Andriushchenko et al., 2025]{andriushchenko2025}
Andriushchenko, M.; and Flammarion, N.
Adaptive Jailbreaking of LLMs.  {\em ICLR}, 2025.

\bibitem[Baysal et al., 2026]{baysal2026}
Baysal, M.; et al.
Asimov Safety Architecture (ASA), {\em IETF draft-baysal-asimov}, 2026.

\bibitem[Besanson et al., 2026]{besanson2026}
Besanson, G.; et al.
SARC: A Governance-by-Architecture Framework for Agentic AI.
{\em arXiv:2605.07728}, 2026.

\bibitem[Ge et al., 2026]{ge2026}
Ge, J.; et al.
Governance Architecture for Autonomous Agent Systems.
{\em arXiv:2603.07191}, 2026.

\bibitem[Hu et al., 2026]{hu2026}
Hu, D.; et al.
Layer-Isolated Evaluation: Gating the Deterministic Scaffold.
{\em arXiv:2606.11686}, 2026.

\bibitem[Li et al., 2026]{li2026}
Li, J.; et al.
CAAF: Harness as an Asset.  {\em arXiv:2604.17025}, 2026.

\bibitem[OWASP, 2025]{owasp2025}
OWASP Foundation.  LLM01:2025---Prompt Injection.  2025.

\bibitem[Santos, 2026]{santos2026}
Santos, R.  Deterministic Guardrails for Enterprise Agents.
{\em Zenodo:19053079}, 2026.

\bibitem[Tallam, 2026]{tallam2026}
Tallam, S.  A Five-Plane Reference Architecture.
{\em arXiv:2606.12320}, 2026.

\bibitem[Watkins et al., 2026]{watkins2026}
Watkins, R.; et al.
Position: A Three-Layer Assume-Guarantee Architecture.
{\em arXiv:2605.18672}, 2026.

\bibitem[Yang et al., 2026]{yang2026}
Yang, T.; et al.
Provably Secure Agent Guardrail.  {\em arXiv:2605.29251}, 2026.

\bibitem[Zhao et al., 2025]{zhao2025}
Zhao, S.; et al.
ShieldAgent: Shielding Agents via Verifiable Safety Policy Reasoning.
{\em arXiv:2503.22738}, 2025.

\end{thebibliography}

\end{document}
